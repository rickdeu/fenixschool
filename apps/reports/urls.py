"""Rotas da app `reports`.

Incluído em `config.urls` (ou num app agregador) quando a app tiver views implementadas.
"""

from django.urls import path

app_name = "reports"

urlpatterns: list[path] = []
