"""Fixtures partilhadas de pytest para os testes de topo (`tests/unit`, `tests/integration`,
`tests/sync_partition`) — ver docs/13-testes-e-qualidade.md.

Os testes específicos de cada app Django vivem em `apps/<app>/tests/` e usam as suas
próprias fixtures locais quando necessário.
"""
