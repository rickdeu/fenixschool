// JavaScript próprio do FenixSchool -- o comportamento do layout (sidebar,
// menus) já vem de static/vendor/kiaalap/main.js. Mantém-se vazio até haver
// uma necessidade real de comportamento próprio.
