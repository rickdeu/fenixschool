# Dependências base do projecto, comuns a todos os ambientes (dev, Nó Local, Nó
# Central e testes) — ver docs/10-stack-tecnologica-e-estrutura-projeto.md §10.1.
Django==5.2.17
djangorestframework==3.18.1
psycopg[binary]==3.3.5

# Reads settings from environment variables (and an optional local `.env`
# file) instead of hardcoding secrets/config in source -- see
# docs/09-seguranca-e-privacidade.md §9.7 and `.env.example`.
django-environ==0.14.0

# Structured (JSON), locally-rotated logging, no external service required --
# RNF-OBS-01 (docs/03-requisitos-nao-funcionais.md §3.11) and
# config/settings/base.py's "Logging" section.
structlog==26.1.0
django-structlog==10.1.0

# Time-sortable UUID v7 for SyncedModel primary keys (Python's stdlib `uuid` does
# not implement uuid7() yet) — see docs/04-arquitetura-tecnica.md §4.5 and
# apps/core/models/base.py.
uuid6==2025.0.1

# Required by Django's ImageField (validates and reads image dimensions) --
# used by core.Institution.logo (docs/05-modelo-de-dados.md §5.2, issue #15).
Pillow==12.3.0
