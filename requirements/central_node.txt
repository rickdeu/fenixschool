# Dependências adicionais de produção para o Nó Central — ver
# `config/settings/central_node.py` e docs/10-stack-tecnologica-e-estrutura-projeto.md
# §10.1/§10.3.
-r base.txt

celery==5.6.3
redis==8.1.0
whitenoise==6.12.0
gunicorn==26.2.0

# Optional error reporting (RNF-OBS-03) -- only activated when SENTRY_DSN is
# set, see config/settings/central_node.py. Not a local_node dependency at
# all: that node must never require connectivity to a third-party service.
sentry-sdk==2.69.1
